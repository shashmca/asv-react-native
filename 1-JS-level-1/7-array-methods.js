


let products = [
    { id: 1, name: 'item-1', price: 2000.00 },
    { id: 2, name: 'item-2', price: 3000.00 },
    { id: 3, name: 'item-3', price: 3000.00 },
    { id: 4, name: 'item-4', price: 4000.00 }
]


// let newItem = { id: 2, name: 'item-2', price: 3000.00 }

// - push
// products.push(newItem)

// - concat
// let result = products.concat(newItem)

// - slice
// let result = products.slice(1, 3);

// - splice
// products.splice(2, 1)
// products.splice(2, 1, { id: 33, name: 'Item-33', price: 3300 })

// - every
// let result = products.every(item => item.price > 5000)

// - some
// let result = products.some(item => item.price > 3000)

// - filters
// let result=products.filter(item => item.price > 3000)


// - map
// let result = products.map(item => item.name)
// let result = products.map(item => item.price)
// let result = products.map(item => item.price > 3000 ? item : null)
// let result = [1, 2, 3, 4, 5].map(n => n * n)

// - reduce
// let result = products
//     .map(item => item.price)
//     .reduce((acc, next) => acc + next)


// 
// let menuString = ["idly", "vada", 'poori'].join(",")F

// 